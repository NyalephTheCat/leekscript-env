# Nix `applications.rust` often sets ~/.cargo `linker` to a gcc-wrapper that breaks rustup's lld shim.
# This matches a working shell: plain gcc + BFD (see .config/config.toml for the bfd half).
export RUSTFLAGS="-C linker=gcc -C link-arg=-fuse-ld=bfd"
