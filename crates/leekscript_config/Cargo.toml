[package]
name = "leekscript_config"
version = "0.1.0"
edition.workspace = true
publish = false
description = "Leek.toml parsing and validation"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
toml = "0.8"
