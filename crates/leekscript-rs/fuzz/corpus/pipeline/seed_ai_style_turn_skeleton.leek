// leeklang: version=v4
// Inspired by phased turn flow without platform calls: ordered steps and a simple metric.
var phase = 0;
var weight = 0;
function step(integer w) {
    phase = phase + 1;
    weight = weight + w;
}
step(3);
step(5);
step(8);
return weight + phase;
