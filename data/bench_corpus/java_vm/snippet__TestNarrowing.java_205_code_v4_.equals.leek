// leek-version: 4
// leek-strict: false
class Item {} class Chip extends Item { boolean flag = true } class Holder { Item item = new Chip(); test() { if (item instanceof Chip && item.flag) { return 1 } return 0 } } return new Holder().test()
