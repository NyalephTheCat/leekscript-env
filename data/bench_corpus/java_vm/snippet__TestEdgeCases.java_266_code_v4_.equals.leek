// leek-version: 4
// leek-strict: false
function buildTree(depth) { if (depth == 0) return ['leaf': true, 'data': [1, 2, 3]] return ['left': buildTree(depth - 1), 'right': buildTree(depth - 1), 'data': [depth]] } var tree = buildTree(3) return tree['data'][0]
