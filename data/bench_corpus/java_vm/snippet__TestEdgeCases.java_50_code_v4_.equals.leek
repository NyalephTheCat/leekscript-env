// leek-version: 4
// leek-strict: false
function minimax(depth, isMax) { if (depth == 0) return randInt(0, 100) var scores = [] for (var i = 0; i < 3; i++) push(scores, minimax(depth - 1, !isMax)) if (isMax) return arrayMax(scores) else return arrayMin(scores) } return minimax(4, true) >= 0
