// leek-version: 4
// leek-strict: false
class B { public integer x = 0 } class A { private check(B | null b) { return b != null ? b.x : -1 } public run() { return check(null) + ',' + check(new B()) } } return new A().run()
