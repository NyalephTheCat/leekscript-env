// leek-version: 4
// leek-strict: false
var results = [] for (var round = 0; round < 8; round++) { var scores = [] for (var i = 0; i < 100; i++) push(scores, randInt(0, 1000)) push(results, arrayMax(scores)) } return count(results)
